export default class RickMortyPagination {
  // eslint-disable-next-line no-useless-constructor
  constructor(public count: number, public pages: number, public next: number, public prev: number) {}
}
